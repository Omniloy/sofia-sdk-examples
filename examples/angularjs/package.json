{
  "name": "sofia-angularjs",
  "version": "0.0.2",
  "description": "Sofia SDK • AngularJS Integration Example - Professional development environment with dynamic configuration and real-time debugging tools",
  "author": "Omniloy",
  "license": "MIT",
  "scripts": {
    "build": "echo 'No build step required. Copy environment.example.json to environment.json and configure.'",
    "start": "http-server ./src -a 0.0.0.0 -p ${PORT:-8000} -c-1 --cors",
    "dev": "http-server ./src -a localhost -p 8000 -c-1 -o"
  },
  "dependencies": {
    "@omniloy/sofia-sdk": "^1.0.4",
    "angular": "^1.8.3",
    "angular-route": "^1.8.3",
    "http-server": "^14.1.1"
  }
}